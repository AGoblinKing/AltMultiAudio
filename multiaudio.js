var MultiAudio = (function() {
    "use strict";
    
    return function(source, opts) {
        opts = opts || {};
        opts.size = opts.size || 100; // Buffer Size (100ms)
        
    };
} ());